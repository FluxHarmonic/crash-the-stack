# Reserved marks

Everything under `assets/` is CC-BY-4.0 (see `assets/LICENSE`) except the items
listed here, which are reserved and not licensed for reuse:

- The name "Crash The Stack"
- The logo
- The key art (title screen, storefront capsule)

The tile faces, card faces, palette, HUD, sound effects and music are not
reserved. Each asset's origin is recorded in `assets/MANIFEST` as it lands.
