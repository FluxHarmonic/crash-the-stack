source: motif (motif examples ab-tracker), the ab-tracker-new render, at motif v0.5.3
  (`motif render ab-tracker-new -o out.wav` reproduces it byte for byte from that tag;
  the render used here is the motif worker's of 2026-09-18, ~/Ops/artifacts/motif-fm-voice/ab-tracker-new.wav,
  sha256 9ec7628ffc9f8a92... , 25.6 s stereo 44100 s16, four passes of a 16-beat pattern at 150 BPM)
shaping: ffmpeg -i ab-tracker-new.wav -af "afade=t=out:st=25.57:d=0.03" -ac 1 -ar 22050 -f s16le ambient.pcm
  (mono, 22050 Hz, s16le, no header: 1,128,960 bytes = 564,480 frames = 25.6 s; the 30 ms fade at
  the end takes the loop boundary from -4096 to -5 so the loop does not click; the pattern is
  bar-aligned at 25.6 s so the loop lands on the downbeat)
ruled: the leader, 2026-09-18 (D28 sequencing: the tracker's own soundtrack replaces this file later,
  same path, same format, a manifest row and no code)
